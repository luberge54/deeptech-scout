# Python
__pycache__/
*.py[cod]
.venv/

# Secrets — never commit
.env

# Collected data and generated reports. data/raw is step 2's input directory -
# it is globbed, so nothing that is not a company report may live there.
data/raw/
data/output/
data/scored/

# data/archive is deliberately NOT ignored: it holds evidence kept on purpose,
# such as the discarded 12-search ANYbotics run behind section 6 of the scoping
# doc. It is never read by the pipeline.

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
